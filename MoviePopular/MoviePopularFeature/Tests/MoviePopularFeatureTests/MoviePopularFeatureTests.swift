import XCTest
@testable import MoviePopularFeature

final class MoviePopularFeatureTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MoviePopularFeature().text, "Hello, World!")
    }
}
