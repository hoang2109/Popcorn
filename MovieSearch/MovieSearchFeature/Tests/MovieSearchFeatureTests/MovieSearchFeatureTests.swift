import XCTest
@testable import MovieSearchFeature

final class MovieSearchFeatureTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MovieSearchFeature().text, "Hello, World!")
    }
}
